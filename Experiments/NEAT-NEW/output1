/Users/rodrigocanaan/anaconda3/lib/python3.6/site-packages/h5py/__init__.py:34: FutureWarning: Conversion of the second argument of issubdtype from `float` to `np.floating` is deprecated. In future, it will be treated as `np.float64 == np.dtype(float).type`.
  from ._conv import register_converters as _register_converters
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;['/Users/rodrigocanaan/Dev/hanabi-learning-environment/agents/NEAT-NEW', '/Users/rodrigocanaan/Dev/hanabi-learning-environment', '/Users/rodrigocanaan/anaconda3/lib/python36.zip', '/Users/rodrigocanaan/anaconda3/lib/python3.6', '/Users/rodrigocanaan/anaconda3/lib/python3.6/lib-dynload', '/Users/rodrigocanaan/anaconda3/lib/python3.6/site-packages', '/Users/rodrigocanaan/anaconda3/lib/python3.6/site-packages/aeosa', '/Users/rodrigocanaan/Dev/spawningtool/src/spawningtool', '/Users/rodrigocanaan/anaconda3/lib/python3.6/site-packages/neat_python-0.92-py3.6.egg']

	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
Warning: initial_connection = partial with hidden nodes will not do direct input-output connections;
	if this is desired, set initial_connection = partial_nodirect 0.5;
	if not, set initial_connection = partial_direct 0.5
